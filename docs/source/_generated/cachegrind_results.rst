.. cachegrind-results — placeholder until Cachegrind CI runs on main.

.. note::

   Cachegrind numbers are produced by ``scripts/run_cachegrind_bench.sh`` on
   GitHub Actions (workflow **Cachegrind benchmarks**). After the first
   successful run on ``main``, this include is replaced with a table of
   instruction counts (``I refs``). Locally: install Valgrind, then run the
   script (several minutes).
