# readcon-core branding

- `readcon-logo-light.svg` / `readcon-logo-dark.svg` — horizontal logotype for docs (shibuya light/dark).
- Sphinx copies live under `docs/source/_static/` (`logo-light.svg`, `logo-dark.svg`, `favicon.svg`).

Mirrored from the rsx-rs docs pattern (shibuya `light_logo` / `dark_logo`).
